export interface User {
    id: string;
    email: string;
    firstName: string;
    lastName: string;
    city: string;
    address: string;
    tracks_balance: number;
    tracks_loyalty_number: string;
}